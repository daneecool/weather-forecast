name: Python application and GitHub Pages update

on:
  schedule:
    - cron: '*/30 * * * *'  # Changed to every 30 minutes to avoid rate limits
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:  # Allow manual triggering

permissions:
  contents: write
  actions: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Fetch all history for all branches

    - name: Set up Python 3.11
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    - name: Lint with flake8
      run: |
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    - name: Run preprocess.py
      run: |
        cd src
        python preprocess.py

    - name: Check if JSON files were created
      run: |
        ls -la src/*.json || echo "No JSON files found"

    - name: Checkout gh-pages branch
      uses: actions/checkout@v4
      with:
        ref: gh-pages
        path: gh-pages
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Copy JSON files to gh-pages
      run: |
        cp src/*.json gh-pages/ || echo "No JSON files to copy"

    - name: Commit and push changes
      run: |
        cd gh-pages
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add *.json || echo "No JSON files to add"
        if git diff --staged --quiet; then
          echo "No changes to commit"
        else
          git commit -m "Update JSON files [skip ci]"
          git push origin gh-pages
        fi
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  cleanup-old-workflows:
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule'  # Only run cleanup on scheduled runs
    steps:
      - name: Delete workflow runs older than 7 days
        uses: Mattraks/delete-workflow-runs@v2
        with:
          retain_days: 7
          keep_minimum_runs: 10
          token: ${{ secrets.GITHUB_TOKEN }}

# ----------------------------------------------------------------------------------------------------------------------------- #

# ----- #

# name: Python application and GitHub Pages update

# on:
#   schedule:
#     - cron: '0 */6 * * *'
#   push:
#     branches: [ "main" ]
#   pull_request:
#     branches: [ "main" ]

# permissions:
#   contents: write
#   actions: write

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest

#     steps:
#     - uses: actions/checkout@v4

#     - name: Set up Python 3.10
#       uses: actions/setup-python@v4
#       with:
#         python-version: "3.10"

#     - name: Install dependencies
#       run: |
#         python -m pip install --upgrade pip
#         pip install flake8
#         if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

#     - name: Lint with flake8
#       run: |
#         flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
#         flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

#     - name: Run preprocess.py
#       run: python src/preprocess.py

#     - name: Checkout gh-pages branch
#       uses: actions/checkout@v4
#       with:
#         ref: gh-pages
#         path: gh-pages

#     - name: Copy JSON files to gh-pages
#       run: |
#         cp src/*.json gh-pages/

#     - name: Commit and push changes
#       run: |
#         cd gh-pages
#         git config user.name "github-actions[bot]"
#         git config user.email "github-actions[bot]@users.noreply.github.com"
#         git add *.json
#         git commit -m "Update JSON files [skip ci]" || echo "No changes to commit"
#         git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}
#         git push origin gh-pages
#       env:
#         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

#   cleanup-old-workflows:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Delete all workflow runs
#         uses: Mattraks/delete-workflow-runs@v2
#         with:
#           retain_days: 0
#           keep_minimum_runs: 0
#           token: ${{ secrets.PAT_TOKEN }}
